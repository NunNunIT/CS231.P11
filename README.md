# Tạo môi trường
``` python -m venv venv ```

# Kích hoạt môi trường
- Với Linux/MacOS
```venv/bin/activate```

- Với Windows
```venv\Scripts\activate   ```

# Cài đặt thư viện
``` pip install -r requirements.txt ```


# Hủy kích hoạt
``` deactivate ```
